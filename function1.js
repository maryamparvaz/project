// تابعی بنویسید که عددی را دریافت کرده و زوج یا فرد بودن آن را نمایش دهد

const x = function () {
    vorodi = Number(prompt('yek add vared konid'))
    if (vorodi % 2 == 0) {
        alert ('add zog ast')
    } else {
        alert('add fard ast')
     }
}

x();
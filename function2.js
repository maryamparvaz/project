// تابعی بنویسید که سه عدد رو بگیره و میانگین اونا رو محاسبه کرده و نشون بده

function avrage() {
    x = Number(prompt('add aval ra vared konid'));
    y = Number(prompt('add dovom ra vared konid'));
    z = Number(prompt('add sevom ra vared konid'));
    let avvrage = (x + y + x) / 3;
    alert(avvrage);
}
avrage()